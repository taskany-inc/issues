# TODO
feature: Команда

spec:
  Страница команды:
    сокращение названия:
      - assert: название сократилось с помощью "..."

    удалить команду:
      # еще не реализовано
      # https://github.com/taskany-inc/issues/issues/652
