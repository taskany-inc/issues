import styled from 'styled-components';

import { gray4 } from '../design/@generated/themes';

export const PageSep = styled.div`
    border-top: 1px solid ${gray4};
    margin: 16px -40px; /* Page component padding */
`;
