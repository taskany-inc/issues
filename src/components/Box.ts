import styled from 'styled-components';
import { gapXs, gray4, radiusS } from '@taskany/colors';

export const Box = styled.div`
    padding: ${gapXs};
    border: 1px solid ${gray4};
    border-radius: ${radiusS};
    width: 100%;
`;
